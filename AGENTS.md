# Agent 协作规范

本文件约束所有在本仓库中工作的 AI Agent。若子目录存在更具体的 `AGENTS.md`，仅在不削弱本文件安全规则的前提下采用更具体规则。

## 沟通与证据

- 默认使用中文沟通，代码、命令、路径和技术名词保留英文。
- 将用户描述视为待验证假设；先检查仓库状态、相关文件、调用链、配置、测试和日志。
- 明确区分事实、推断、假设、未知项和风险，不把编译通过等同于功能验证完成。
- 在修改前说明目标文件、原因、影响、风险、验证方式和回滚方式。

## 修改边界

- 优先查看 `git diff`，只做达成目标所需的最小修改。
- 禁止无关重构、全仓格式化、批量改名、依赖升级和构建配置迁移，除非用户明确授权。
- 保留用户已有修改；发现脏工作区时先识别变更归属，不覆盖、不丢弃。
- 不写入凭据、Token、证书、Cookie、私钥或真实生产数据。

## Git 安全规则

- Commit Message 默认使用中文，并遵循 Conventional Commits。
- `git add` 前必须展示并分析工作区差异；`git commit` 前必须展示并分析暂存区差异。
- `push`、`merge`、`rebase`、任何 `reset` 必须在执行前单独获得用户确认；先前对其他步骤的确认不能复用。
- 禁止执行 `git push --force`、`git push -f`、`git reset --hard` 和任何等价的强制覆盖操作。
- 禁止执行 `rm -rf`；需要删除时必须解析精确目标、说明恢复能力并获得确认。
- 不自动创建、改写或删除远端分支、Tag、Release。

## Skill 规范

- Skill 目录名必须与 `SKILL.md` 的 `name` 一致，使用小写字母、数字和连字符。
- `SKILL.md` frontmatter 只放 `name` 与 `description`；description 同时说明能力、触发场景和边界。
- 正文使用命令式表达，保持聚焦；详细规则放入 `shared/`，脚本放入 `scripts/`。
- `agents/openai.yaml` 的 `default_prompt` 必须显式包含 `$skill-name`。
- 示例必须使用虚构分支、提交和路径，不含秘密信息或危险命令的可执行诱导。

## Shell 规范

- 脚本兼容 macOS 默认 Bash 3.2 与主流 Linux Bash，不依赖 GNU 专属参数。
- 至少启用 `set -u`；对 Git 与搜索命令的预期非零退出码显式分支处理，不依赖 `set -e` 隐式终止。确需管道错误传播时使用 Bash 并启用 `set -o pipefail`。
- 路径始终加双引号；临时目录使用 `mktemp -d` 并注册清理函数。
- 默认不改变工作区内容、refs 或暂存内容；分析脚本不得隐式执行 commit、push、merge、rebase、reset 或文件删除。Git 状态类命令可能刷新 index 的 stat 元数据，文档不得承诺字节级只读。
- 错误写入 stderr，成功、警告和阻断使用稳定退出码，并在脚本帮助中说明。

## 验证与报告

- 运行最小且相关的验证：Skill 校验、`bash -n`、`shellcheck`、目标脚本行为测试和 Markdown 链接检查。
- 未实际运行的验证不得宣称通过；环境缺少工具时说明边界并给出可复现命令。
- 最终报告必须包含：变更内容、变更原因、修改文件、已执行验证、剩余风险、回滚步骤。
