# 安全策略

## 支持范围

当前维护线为 `0.1.x`。安全修复优先发布到最新版本；历史版本不保证单独回补。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告，不要创建公开 Issue。报告应包含：

- 受影响的 Skill、脚本、版本或提交；
- 可复现的最小步骤与运行环境；
- 实际影响和最坏影响；
- 证明材料中已脱敏的输出；
- 可选的修复建议或缓解方式。

不要在报告中放入真实 Token、私钥、Cookie 或生产数据。需要展示秘密形态时使用 `[REDACTED_SECRET]`。

## 响应流程

维护者会先确认报告是否收到，再复现、定级、制定修复和披露计划。涉及强制覆盖、任意命令执行、秘密泄露或绕过危险操作确认的缺陷按 Critical 优先处理。

## 安全设计边界

本仓库检查脚本不改变工作区内容、refs 或暂存内容；Git 状态类命令可能刷新 index 的 stat 元数据，因此不承诺字节级只读。脚本输出的 Git 建议仍需操作者理解后执行。Agent 的确认文本不是操作系统级权限隔离；生产仓库应继续使用分支保护、最小权限凭据、签名提交、CI 门禁和备份。
